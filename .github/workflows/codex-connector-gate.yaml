name: Codex Connector Gate

# Disabled while Claude Code handles pull request review.
on:
  workflow_dispatch:

permissions: {}

jobs:
  codex-connector-status:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Report disabled status
        run: echo "Codex connector gate is disabled."
