apiVersion: v2
name: argocd
description: A Helm chart for Kubernetes
type: application
version: 0.1.1
icon: https://raw.githubusercontent.com/helm/helm/main/assets/images/helm.svg
dependencies:
- name: argo-cd
  # 2026-08-13: argo-cd 10.2.3+ rejects --disable-tls when --client-ca-path
  # defaults to a non-empty path (argoproj/argo-cd#26715), CrashLoopBackOff.
  # Workaround: explicit --client-ca-path= flag in values.yaml
  # repoServer.extraArgs.
  version: 10.9.2
  repository: https://argoproj.github.io/argo-helm
