apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: keda-operator-metrics-apiserver
  namespace: keda
spec:
  # 2026-07-26: repo-wide PDB audit. The vendored chart's native PDB option
  # only covers the operator -- metricServer has none, so hand-written here.
  minAvailable: 1
  selector:
    matchLabels:
      app: keda-operator-metrics-apiserver
