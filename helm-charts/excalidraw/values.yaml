name: excalidraw
namespace: excalidraw

# VPA 2026-07-30: memory-only. 7d usage was 7-8Mi vs a 128Mi request, no incident history.
# CPU stays with the HPA.
# Memory trigger moved to AverageValue (see hpa.yaml) to decouple it from the VPA-adjusted request -- blocky's HPA needed the same fix.
vpa:
  enabled: true

deployment:
  image:
    repository: docker.io/excalidraw/excalidraw
    tag: latest@sha256:f7ee194addd607bf831d2af0f0a34463dd4225e426cf35199ef0b12a803398e9

service:
  port: 80
  targetPort: 80
