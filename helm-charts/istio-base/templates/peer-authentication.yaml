apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: {{ .Values.namespace }}
spec:
  mtls:
    # Start in permissive mode while ambient enrollment is rolled out and
    # verified, then tighten to STRICT once webhook and system paths are known.
    mode: PERMISSIVE
